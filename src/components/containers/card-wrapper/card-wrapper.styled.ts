import styled from 'styled-components';

export const CardWrapperContainer = styled.article`
  background: #fff;
  box-shadow: 0 2px 3px rgba(0, 0, 0, 0.108696);
  border-radius: 2px;
`;

export default {
  CardWrapperContainer,
};
