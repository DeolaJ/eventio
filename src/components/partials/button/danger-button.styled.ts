import styled from 'styled-components';
import BaseButton from './base';

export const DangerButtonContainer = styled(BaseButton)`
  background-color: var(--color-red);
`;

export default {
  DangerButtonContainer,
};
